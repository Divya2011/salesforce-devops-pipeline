name: Deploy to Production (Hotfix + Auto Back-Sync)

on:
  push:
    branches:
      - main
    paths:
      - "force-app/**"
  workflow_dispatch:

# Configure this in GitHub → Repo → Settings → Variables → SYNC_BRANCHES
# Example value: preprodbranch uatbranch feature
env:
  SYNC_BRANCHES: ${{ vars.SYNC_BRANCHES || 'preprodbranch uatbranch feature' }}

jobs:

  ###########################################################################
  # 1️⃣ DEPLOY TO PRODUCTION
  ###########################################################################
  deploy-production:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Salesforce CLI
        run: npm install -g @salesforce/cli

      - name: Update Salesforce CLI
        run: sf update

      - name: Authenticate to Production
        run: |
          echo "${{ secrets.PROD_SFDX_AUTH_URL }}" > prod_auth.txt
          sf org login sfdx-url -f prod_auth.txt --alias prodorg --set-default

      - name: Deploy to Production
        run: |
          sf project deploy start \
            --source-dir force-app \
            --test-level RunLocalTests \
            --ignore-conflicts \
            --wait 40 \
            -o prodorg


  ###########################################################################
  # 2️⃣ DETECT HOTFIX MERGE (STRICT)
  ###########################################################################
  detect-hotfix:
    runs-on: ubuntu-latest
    needs: deploy-production
    outputs:
      is_hotfix: ${{ steps.check_hotfix.outputs.is_hotfix }}

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Detect Hotfix Merge (Strict Pattern)
        id: check_hotfix
        run: |
          COMMIT_MSG="$(git log -1 --pretty=%B)"
          echo "Commit message:"
          echo "$COMMIT_MSG"

          # STRICT hotfix detection
          if [[ "$COMMIT_MSG" == *"Merge pull request"* && "$COMMIT_MSG" == *"from hotfix/"* ]]; then
            echo "is_hotfix=true" >> $GITHUB_OUTPUT
            echo "🔥 Hotfix merge detected — enabling back-sync."
          else
            echo "is_hotfix=false" >> $GITHUB_OUTPUT
            echo "⛔ Not a hotfix — skipping back-sync."
          fi


  ###########################################################################
  # 3️⃣ AUTO BACK-SYNC TO LOWER ENVIRONMENT BRANCHES
  ###########################################################################
  backsync:
    runs-on: ubuntu-latest
    needs: detect-hotfix
    if: needs.detect-hotfix.outputs.is_hotfix == 'true'

    strategy:
      fail-fast: false
      # convert "preprodbranch uatbranch feature" → ["preprodbranch","uatbranch","feature"]
      matrix:
        branch: ${{ fromJson(vars.SYNC_BRANCHES) }}

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup NodeJS (required by sync-branches)
        uses: actions/setup-node@v3
        with:
          node-version: 12

      - name: Sync main → ${{ matrix.branch }}
        uses: tretuna/sync-branches@1.4.0
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FROM_BRANCH: "main"
          TO_BRANCH: ${{ matrix.branch }}
          PULL_REQUEST_AUTO_MERGE_METHOD: merge
